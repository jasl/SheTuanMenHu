<h1>Listing pages</h1>

<table>
  <tr>
    <th>Title</th>
    <th></th>
  </tr>

<% @pages.each do |page| %>
  <tr>
    <td><%= page.title %></td>
    <td><%= link_to 'Show', group_page_path(page.group.permalink,page) %></td>
  </tr>
<% end %>
</table>
